#include <bits/stdc++.h>
using namespace std;

void warshall(vector<vector<int>>& dp, size_t l, size_t r)
{
    size_t N = dp.size();
    // split into 4 * 4 or 6 * 6 or 8 * 8
    // and compute 
    // 1. (para) i_mat == j_mat
    // 2. (serial) i1_mat != i2_mat != j1_mat != j2_mat
}

int main()
{
    return 0;
}